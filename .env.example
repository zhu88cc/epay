# Web 对外端口（默认 8080）
APP_PORT=8080

# 外部 MySQL 连接信息（必填）
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=epay
DB_PASS=change_me
DB_NAME=epay

# 表前缀（可选，默认 pay）
DB_PREFIX=pay

# 可选：如果你用反代/固定域名，建议在后台配置 localurl
# SITE_URL=https://pay.example.com/
